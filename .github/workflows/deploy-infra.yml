name: Deploy Infrastructure

on:
  push:
    branches: [main, master]
    paths:
      - 'terraform/**'
      - '.github/workflows/deploy-infra.yml'
  pull_request:
    branches: [main, master]
    paths:
      - 'terraform/**'
  workflow_dispatch:
    inputs:
      action:
        description: 'Action to perform'
        required: true
        default: 'plan'
        type: choice
        options:
          - plan
          - apply
          - destroy

permissions:
  contents: read
  security-events: write
  actions: read

env:
  TF_VERSION: '1.6.0'
  AWS_REGION: 'us-east-1'
  TF_VAR_mongodb_admin_pass: ${{ secrets.MONGODB_ADMIN_PASS }}
  TF_VAR_mongodb_app_pass: ${{ secrets.MONGODB_APP_PASS }}
  TF_VAR_backup_encryption_key: ${{ secrets.BACKUP_ENCRYPTION_KEY }}
  TF_VAR_wazuh_admin_password: ${{ secrets.WAZUH_ADMIN_PASS }}
  TF_VAR_wazuh_api_password: ${{ secrets.WAZUH_API_PASS }}
  TF_VAR_container_image: ${{ vars.CONTAINER_IMAGE || 'nginx:latest' }}
  TF_VAR_redteam_allowed_cidrs: '["0.0.0.0/0"]'
  TF_VAR_wazuh_allowed_cidrs: '["0.0.0.0/0"]'

jobs:
  validate:
    name: Validate
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ env.TF_VERSION }}

      - name: Terraform Format Check
        run: terraform fmt -check -recursive
        working-directory: terraform
        continue-on-error: true

      - name: Terraform Init (validation only)
        run: terraform init -backend=false
        working-directory: terraform

      - name: Terraform Validate
        run: terraform validate
        working-directory: terraform

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install MkDocs
        run: pip install mkdocs-material mkdocs-minify-plugin

      - name: Build documentation
        run: mkdocs build --strict

  security-scan:
    name: Security Scan
    runs-on: ubuntu-latest
    needs: validate
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: tfsec scan
        uses: aquasecurity/tfsec-action@v1.0.3
        with:
          working_directory: terraform
          soft_fail: true
        continue-on-error: true

      - name: Checkov scan
        uses: bridgecrewio/checkov-action@v12
        with:
          directory: terraform
          soft_fail: true
          skip_check: CKV_AWS_144,CKV_AWS_145
        continue-on-error: true

  terraform:
    name: Terraform
    runs-on: ubuntu-latest
    needs: [validate, security-scan]
    defaults:
      run:
        working-directory: terraform

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ env.TF_VERSION }}

      - name: Terraform Format Check
        run: terraform fmt -check -recursive
        continue-on-error: true

      - name: Terraform Init
        run: terraform init

      - name: Terraform Validate
        run: terraform validate

      - name: Import existing CloudWatch Log Groups
        if: github.event.inputs.action != 'destroy'
        run: |
          # Import existing log groups into Terraform state if they exist in AWS but not in state
          # This prevents "ResourceAlreadyExistsException" errors

          import_if_exists() {
            local resource_addr=$1
            local log_group_name=$2

            # Check if log group exists in AWS
            if aws logs describe-log-groups --log-group-name-prefix "$log_group_name" \
               --query "logGroups[?logGroupName=='$log_group_name'].logGroupName" \
               --output text 2>/dev/null | grep -q "$log_group_name"; then

              # Check if already in Terraform state
              if ! terraform state show "$resource_addr" >/dev/null 2>&1; then
                echo "Importing existing log group: $log_group_name -> $resource_addr"
                terraform import -var-file="environments/demo.tfvars" "$resource_addr" "$log_group_name" || true
              else
                echo "Log group already in state: $resource_addr"
              fi
            else
              echo "Log group does not exist in AWS: $log_group_name"
            fi
          }

          import_if_exists "module.eks.aws_cloudwatch_log_group.eks" "/aws/eks/wiz-exercise-eks/cluster"
          import_if_exists "module.vpc.aws_cloudwatch_log_group.flow_logs[0]" "/aws/vpc-flow-logs/wiz-exercise"
        continue-on-error: true

      - name: Terraform Plan
        id: plan
        run: |
          terraform plan \
            -var-file="environments/demo.tfvars" \
            -out=tfplan \
            -no-color
        continue-on-error: true

      - name: Upload Plan
        uses: actions/upload-artifact@v4
        with:
          name: tfplan
          path: terraform/tfplan
          retention-days: 5

      - name: Plan Status
        if: steps.plan.outcome == 'failure'
        run: exit 1

      # Apply on push to main/master or manual trigger with 'apply'
      - name: Terraform Apply
        if: |
          (github.event_name == 'push' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master')) ||
          (github.event_name == 'workflow_dispatch' && github.event.inputs.action == 'apply')
        run: terraform apply -auto-approve tfplan

      # Destroy only on manual trigger with 'destroy'
      - name: Terraform Destroy
        if: github.event_name == 'workflow_dispatch' && github.event.inputs.action == 'destroy'
        run: |
          terraform destroy \
            -var-file="environments/demo.tfvars" \
            -auto-approve

      - name: Terraform Output
        if: |
          (github.event_name == 'push' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master')) ||
          (github.event_name == 'workflow_dispatch' && github.event.inputs.action == 'apply')
        run: terraform output -no-color
